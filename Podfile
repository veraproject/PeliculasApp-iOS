# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PruebaTablas' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PruebaTablas

pod 'Alamofire', '~> 4.4'

pod 'SVProgressHUD'

pod 'ReachabilitySwift', '~> 3.0'

  target 'PruebaTablasTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PruebaTablasUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
